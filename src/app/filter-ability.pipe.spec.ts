import { FilterAbilityPipe } from './filter-ability.pipe';

describe('FilterAbilityPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAbilityPipe();
    expect(pipe).toBeTruthy();
  });
});
